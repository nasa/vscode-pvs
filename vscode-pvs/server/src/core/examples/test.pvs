test: theory begin

  vtbi_over_rate_lemma0: LEMMA TRUE 

  vtbi_over_rate_lemma: LEMMA 
    FORALL(v: ivols, r: {r: irates | (NOT r = 0)}): (v / r) >= 0 


  % Subtype TCC generated (at line 28, column 57) for  r
  % expected type  nznum
  % unfinished
%vtbi_over_rate_lemma_TCC1: OBLIGATION
%  FORALL (r:
%            {r:
%               irates
%                   [maxrate, maxinfuse, infusemin, timeout, shorttimeout,
%                    maxtime, bat_max, bat_min] |
%                     (NOT r = 0)}):
%    r /= 0;

        
end test